import {useState, useEffect} from 'react';
import {Button} from '@mui/material';

const GLTFImport = ({module})=>{

    return(
        <Button variant="outlined" sx={{width:'100%'}}>
            gltfimport

        </Button>
    );
}
export default GLTFImport;